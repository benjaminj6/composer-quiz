{
  "name": "composer-quiz",
  "version": "1.0.0",
  "description": "A quiz app that covers composers.",
  "main": "js/app,js",
  "scripts": {
    "mkdir": "mkdir -p build",
    "build": "npm run clean && npm run mkdir && npm run build:html && npm run build:css",
    "clean": "rm -rf build",
    "watch": "npm run watch:html & npm run watch:css",
    "build:html": "npm run clean:html && cp index.html build/",
    "clean:html": "rm -f build/index.html",
    "watch:html": "npm run build:html && chokidar index.html -c 'npm run build:html'",
    "build:css": "npm run clean:css && lessc --source-map css/style.less build/$npm_package_version.css",
    "clean:css": "rm -f build/$npm_package_name.$npm_package_version.css build/$npm_package_name.npm_package_version.map",
    "watch:css": "npm run build:css && chokidar 'css/**/*.less' -c 'npm run build:css'"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/benjaminj6/composer-quiz.git"
  },
  "author": "Benjamin Johnson",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/benjaminj6/composer-quiz/issues"
  },
  "homepage": "https://github.com/benjaminj6/composer-quiz#readme",
  "devDependencies": {
    "chokidar-cli": "^1.2.0",
    "less": "^2.7.1"
  },
  "dependencies": {
    "font-awesome": "^4.6.3",
    "jquery": "^3.1.0",
    "normalize.css": "^4.2.0"
  }
}
